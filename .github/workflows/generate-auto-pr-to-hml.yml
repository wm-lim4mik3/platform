name: Generate Changelog and PR

on:
  push:
    branches:
      - 'feat/PF-*'
      - 'fix/PF-*'

jobs:
  prepare-to-pr:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Fetch HML and Generate Categorized Changelog
        id: changelog
        run: |
          echo "🔄 Iniciando processo de geração de changelog..."

          git fetch origin hml

          CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
          CURRENT_HEAD=$(git rev-parse HEAD)
          HML_COMMIT=$(git rev-parse origin/hml)
          COMMITS_AHEAD=$(git rev-list --count origin/hml..HEAD)

          AUTHOR_NAME=$(git log -1 --pretty=format:'%an')
          LAST_HML_COMMIT=$(git log origin/hml -1 --pretty=format:'%h')

          echo "📍 Branch atual: $CURRENT_BRANCH"
          echo "🔨 HEAD: $CURRENT_HEAD"
          echo "🎯 Target: $HML_COMMIT"
          echo "👤 Autor: $AUTHOR_NAME"

          commits=$(git log origin/hml..HEAD --pretty=format:"%h|%ad|%s" --date=short)

          if [ -z "$commits" ]; then
            echo "✅ Nenhum commit novo encontrado. PR não será criado."
            echo "skip_pr=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          feats=""
          fixes=""
          chores=""
          breaks=""

          while IFS= read -r line; do
            hash=$(echo "$line" | cut -d'|' -f1)
            date=$(echo "$line" | cut -d'|' -f2)
            message=$(echo "$line" | cut -d'|' -f3)

            if [[ $message == feat* ]]; then
              feats+="- $message\n"
            elif [[ $message == fix* ]]; then
              fixes+="- $message\n"
            elif [[ $message == chore* ]]; then
              chores+="- $message\n"
            elif [[ $message == *BREAKING* ]]; then
              breaks+="- $message\n"
            fi

            changelog_updates+="UPDATE [$date] - ${hash:0:6} - $message\n"
          done <<< "$commits"

          changelog=""
          [ -n "$feats" ] && changelog+="### 📦 Feats\n$feats\n"
          [ -n "$fixes" ] && changelog+="### 🛠 Fixes\n$fixes\n"
          [ -n "$chores" ] && changelog+="### 🔧 Chores\n$chores\n"
          [ -n "$breaks" ] && changelog+="### 💥 Breaking Changes\n$breaks\n"

          body_md=""
          body_md+="Este PR foi gerado automaticamente após push na branch feature/correção. Por favor, revise com atenção. 🧐\n\n"
          body_md+="## 📌 Informações do PR\n"
          body_md+="- 🧑‍💻 Autor do push: $AUTHOR_NAME\n"
          body_md+="- 🌿 Branch source: $CURRENT_BRANCH\n"
          body_md+="- 🎯 Branch target: hml\n"
          body_md+="- 📦 Commits à frente de 'hml': $COMMITS_AHEAD commits\n"
          body_md+="- 🧠 HEAD atual: $CURRENT_HEAD\n"
          body_md+="- 🧱 Último commit em 'hml': $LAST_HML_COMMIT\n\n"
          body_md+="---\n\n"
          body_md+="## 🆕 Atualizações\n$changelog_updates\n"
          body_md+="\n$changelog"

          echo "skip_pr=false" >> "$GITHUB_OUTPUT"
          echo "changelog<<EOF" >> "$GITHUB_OUTPUT"
          echo "$body_md" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

      - name: Instalar GitHub CLI
        run: |
          sudo apt update
          sudo apt install -y gh

      - name: Criar ou Atualizar Pull Request
        if: steps.changelog.outputs.skip_pr == 'false'
        run: |
          BRANCH=$(git rev-parse --abbrev-ref HEAD)
          TITLE_ID=$(echo "$BRANCH" | grep -o 'PF-[0-9]*')
          TITLE="[$TITLE_ID] - 🤖 PR Automático"

          gh pr list --head "$BRANCH" --base hml --json number --jq '.[0].number' > pr_number.txt || true
          PR_NUMBER=$(cat pr_number.txt)

          echo "## Gerando corpo do PR"
          echo "${{ steps.changelog.outputs.changelog }}" > pr_body.md

          if [ -n "$PR_NUMBER" ]; then
            echo "✏️ Atualizando PR existente #$PR_NUMBER"
            gh pr edit "$PR_NUMBER" \
              --title "$TITLE" \
              --body-file pr_body.md
          else
            echo "🚀 Criando novo PR"
            gh pr create \
              --title "$TITLE" \
              --body-file pr_body.md \
              --head "$BRANCH" \
              --base hml \
              --label "auto-pr" \
              --reviewer "wm-lim4mik3"
          fi
        env:
          GH_TOKEN: ${{ secrets.PAT }}
